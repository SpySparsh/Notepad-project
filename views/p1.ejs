<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create a New Note</title>
</head>
<body style="background-color: #1e1e1e; color: #f5f5f5; font-family: Arial, sans-serif;">

    <header style="text-align: center; margin-top: 20px;">
        <h1>Create a New Note</h1>
    </header>

    <main style="display: flex; justify-content: center; margin-top: 30px;">
        <section>
            <form action="/create" method="post" id="noteForm" style="width: 100%; max-width: 500px; padding: 20px; background-color: #2d2d2d; border-radius: 8px;">
                <input type="text" id="title" name="filename" placeholder="Enter note title" required
                       style="width: 94%; padding: 8px; background-color: #3c3c3c; color: #f5f5f5; border: none; border-radius: 4px; margin-bottom: 15px;">

                <textarea id="content" name="details" rows="10" placeholder="Write your note here..." required
                          style="width: 94%; height: 200px; padding: 8px; background-color: #3c3c3c; color: #f5f5f5; border: none; border-radius: 4px; resize: none; margin-bottom: 15px;"></textarea>

                <input type="submit" value="Create Note" style="padding: 10px 20px; background-color: #007acc; color: #f5f5f5; border: none; border-radius: 4px; cursor: pointer;">
            </form>
        </section>
    </main>

    <!-- Notes Listing Section -->
    <div style="display: flex; justify-content: flex-start; flex-wrap: wrap; padding: 10px; margin-top: 30px;">
        <% if (files.length > 0) { %>
            <% files.forEach(function(val) { %>
                <div class="task" style="width: 20%; padding: 8px; margin: 15px; background-color: #3c3c3c; color: #f5f5f5; border-radius: 4px;">
                    <h1 style="font-size: large; font-family: 'Franklin Gothic Medium', 'Arial Narrow', Arial, sans-serif;"><%= val %></h1>
                    
                    <!-- Read and Edit Links -->
                    <div style="margin-bottom: 7px;">
                        <a href="/file/<%= val %>" style="color: #848484;">Read</a>
                    </div>
                    <div style="display: flex; justify-content: space-between;">
                    <a href="/edit/<%= val %>" style="color: #b3302b; font-size: 15px; text-decoration: none;">Edit</a>
                    <!-- Delete Form -->
                    <form action="/delete/<%= val %>" method="post" style="display: inline;">
                        <button type="submit" style="background: none; color: #b3302b; border: none; font-size: 15px; cursor: pointer; text-decoration: none;">Delete</button>
                    </form>
                    </div>
                </div>
            <% }) %>    
        <% } else { %>    
            <h3 style="color: #9c9c9c; text-align: center;">No tasks yet.</h3>
        <% } %>    
    </div>

</body>
</html>
